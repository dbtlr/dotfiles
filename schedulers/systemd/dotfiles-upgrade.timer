# Managed by dotfiles (schedulers/systemd/) — installed to
# ~/.config/systemd/user/ by `dot install`.
[Unit]
Description=Run dot upgrade daily at 06:30

[Timer]
OnCalendar=06:30
Persistent=true

[Install]
WantedBy=timers.target
